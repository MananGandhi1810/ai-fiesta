import { AuthContainer } from '@/components/auth/AuthContainer';

export const metadata = {
  title: 'Sign In - AI Fiesta',
  description: 'Sign in to your AI Fiesta account',
};

export default function AuthPage() {
  return <AuthContainer />;
}
